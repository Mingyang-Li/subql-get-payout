1. Given an era and a validator ID,
    retrieve total reward, 
    nom exposure, 
    val exposure (a structure) and,
    commision rate
2. Find events on the chain / ui => to check which block is each payout being claimed

for val: commission + staking money percentage

for nom: 


To run node.js on cmd, use command below:
node --experimental-repl-await

